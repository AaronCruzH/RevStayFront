// This will be an interface defining the type of data I expect to receive from the backend
// You can call this what you want, but I'll call this ICourse for interface course so there's no 
// confusion later
export interface ICourse{
    courseId: number,
    name: string
}